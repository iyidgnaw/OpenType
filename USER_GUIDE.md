# OpenType 使用说明书

适用版本：当前 macOS 开发版（2026-08-09 重写后）

OpenType 是一个运行在 macOS 菜单栏里的本地优先 AI 语音输入工具。按住一个快捷键说话，松开后它会把你说的话转成文字、回答你的问题，或者把一个任务交给 Agent 去做。

它现在只有 **3 种模式**：`听写`、`问答`、`Agent`。语音识别默认完全在本机运行，只有 `问答`、`Agent` 和语音纠错会把文字交给你自己配置的 AI 模型 —— **听写的内容永远不会被发给模型**（§13）。

## 1. 一分钟快速开始

### 第一步：首次启动设置向导

首次启动时，OpenType 会自动打开主窗口并进入设置向导（`欢迎使用 OpenType`）。在两项都配置完成之前，主窗口只显示向导，不会进入正常界面。

1. **语音识别**：选择「本机（MLX-Whisper）」或「远程 API」。本机模式不需要任何 Key，也不联网，直接点「保存」即可。
2. **AI 模型**：选择「Anthropic」或「OpenAI 兼容」，填入 Base URL 和 API Key，点「测试连接」，成功后从模型列表里选一个模型，再点「保存」。

两项都保存后，向导会自动消失，进入正常界面。之后随时可以在 `设置 → 语音识别` 和 `设置 → AI 模型` 里改。

### 第二步：授权

在「输入」标签页顶部的准备卡片里逐项授权：

1. **麦克风**：录制你的语音。
2. **辅助功能**：让全局快捷键生效、读取选中文字、把结果写回输入框。
3. **实时字幕**（可选）：允许 macOS 语音识别，在录音浮层里实时显示你正在说的话。关闭「录音时显示实时字幕」后就不需要这一项。

三项都完成、且快捷键成功注册后，这张卡片会自己消失。

### 第三步：说第一句话

1. 把光标放到目标输入框。
2. 长按左 `Option` 开始说话。
3. 松开左 `Option`，等待识别。
4. 结果会写入当前输入框（如果开启了「完成后自动写入当前输入框」）。

**无论自动写入成功还是失败，最终结果都会保留在剪贴板**，按 `Command + V` 可以随时再粘贴一次。这是产品级保证，不受任何开关影响。

## 2. 快捷键怎么用

在 `设置 → 快捷键 → 启动快捷键` 里选择。可选的方案：

| 方案 | 手势 | 说明 |
| --- | --- | --- |
| **左 Option**（默认） | 长按说话、松开完成；双击开始、按任意普通键结束 | 右 Option 保持空闲 |
| **fn（地球键）** | 与左 Option 完全相同的手势，只是换成 fn 键 | 需要先关掉系统自带的 fn 行为，见下文 |
| 双击 Ctrl | 双击开始录音，按任意普通键结束 | 推荐；两次轻点任意一侧 Ctrl |
| 双击 Option | 同上 | 可能与部分输入法的 Option 快捷键冲突 |
| 双击 Shift | 同上 | 适合不常连续按 Shift 的人 |
| ⌃⇧ Space | 授权辅助功能后：按一次开始、按任意普通键结束；未授权时：按住说话、松开完成 | 不占用 Option；是唯一一个没有辅助功能权限也能用的方案 |
| ⌥ Space / ⌃ Space / ⌃⌥ Space | 按一次开始；授权辅助功能后按任意普通键结束 | 传统组合键方案 |

### 长按：适合短输入

微信回复、一两句话、短邮件、搜索词、临时想法。

1. 把光标放到目标输入框。
2. 长按左 `Option`（或 fn）。
3. 说话。
4. 松开。

松开后立刻开始识别和处理。

### 双击：适合连续录音

较长的想法、文章段落、会议笔记、需要边想边说的内容。

1. 连续轻按两次左 `Option`（或 fn）。
2. 松开手，继续自然说话。
3. 按任意普通键结束录音。

用于结束的那个按键会被 OpenType 拦截，不会输入到文本框里。

唯一的例外是 `Tab`：**只有在你同时按住录音快捷键的时候**，它才不结束录音，而是切换模式（见下一节）；手没按着快捷键时，`Tab` 和别的键一样会结束这次录音。

### 用快捷键切换模式

只有一条规则：**按住你自己那个录音快捷键的同时，点一下 `Tab`**，模式会依次循环：

`听写 → 问答 → Agent → 听写`

「你自己那个录音快捷键」就是设置里选的那个：默认预设按住左 `Option`，改成 fn 就按住 fn，改成双击 Ctrl 就按住任意一侧 `Ctrl`（双击 Option / 双击 Shift 同理）。菜单栏 popover 里的那行提示会直接写出当前预设该按住哪个键。

`Tab` 永远会被 OpenType 拦下，不会传给当前应用。

**录音过程中也可以切换，而且切的就是这一次录音。** 这一点是刻意的：`Tab` 不取消任何东西，所以到底发生什么只取决于你按住多久——

- 点完 `Tab` 就松手（不到 0.3 秒的长按阈值）：只换了模式，没有开始录音。
- 点完 `Tab` 继续按住说话：录音照常进行，并且**按切换后的新模式处理**。浮窗上的听写胶囊会当场变成新模式，不会被「已切换模式」提示挡住。

上面两条说的是**长按**开始的录音。如果这次是**双击开始的免手动录音**，你的手已经离开快捷键了——这时想换模式，要先把那个键**按住**再点 `Tab`。按住本身不会开始第二次录音，点完 `Tab` 再松开也不会停掉正在进行的这次；但如果不按住就直接点 `Tab`，它会被当成「任意普通键」，把这次录音结束掉。

未在录音时切换，等于设置下一次录音要用的模式，屏幕上会显示「已切换模式」和模式名称。

只有 `⌃⇧ Space` 这类空格组合键预设没有这个手势（它们走的是 Carbon 热键，没有修饰键事件监听）——这时菜单栏里也不会显示那行提示。用菜单栏 popover 或主窗口里的模式卡片切换即可。录音进行中另一个改模式的办法是开头说一句「agent 模式」口令（见第 8 节）。

### 使用 fn 键前需要改一个系统设置

fn 同时也是 macOS 自己的功能键（切换输入法、听写、emoji 面板）。OpenType 不会吞掉这个事件，所以系统自带的动作会和 OpenType 一起触发。

建议把 `系统设置 › 键盘 › 按下 🌐 键时` 改成「不执行任何操作」。

### 为什么有时显示 ⌃⇧ Space

左 `Option`、fn 和所有双击类方案都依赖 macOS 的辅助功能权限。权限尚未生效时，OpenType 会临时回退到 `⌃⇧ Space`（按住说话、松开完成），保证仍然可用。设置里的快捷键状态行会明确写出「目标：… · 当前回退：…（等待辅助功能授权）」。

授权完成后重新选择一次快捷键即可恢复。

## 3. 三种模式分别解决什么问题

| 模式 | 需要选中文字？ | OpenType 会做什么 | 是否经过 AI 模型 |
| --- | --- | --- | --- |
| **听写** | 不需要 | 把你说的话原样转成文字 | 听写本身完全不经过；只有你主动发起的语音纠错那一步会经过（直接模式的就地纠错、复核面板里的纠错） |
| **问答** | 不需要 | 直接回答你口头提出的问题，答案显示在浮窗里 | 是 |
| **Agent** | 不需要 | 把任务下发给 Agent Runtime，它可以调用工具，完成后通知你 | 是 |

三种模式都**不需要预先选中文字**：把光标放好、按快捷键说话就行。

三种模式的结果都会复制到剪贴板。**没有任何模式会替你按回车、发送或发布。**

### 录音时的实时字幕

开始录音后，「正在听」浮层会实时显示当前识别到的文字。左侧呼吸光表示正在录音，右侧声波跟随真实说话音量变化。

这里显示的是 Apple 本机识别的低延迟临时预览，识别过程中可能回改前面的文字。它只负责让你确认麦克风听到了什么，**不会成为最终结果**。结束录音后，OpenType 仍会把完整音频交给你在「语音识别」里配置的服务（默认本机 MLX-Whisper）重新识别一次。

可以在 `设置 → 输入体验 → 录音时显示实时字幕` 关闭，关闭后不影响正常语音输入，也不再需要语音识别权限。

## 4. 听写（直接模式）

### 适用情景

- 记录访谈、会议或接近原始状态的素材。
- 需要保留说话人的原句和用词。
- 口述专有名词、数字、引用，或之后准备人工编辑的素材。
- 任何你**不希望 AI 改动一个字**的场合。

### 它会做什么

把录音交给语音识别，识别到什么就是什么，然后直接交付：复制到剪贴板，并在开启「完成后自动写入当前输入框」时写入光标位置。

### 它不会做什么

- 不会调用任何 AI 模型 —— 这条路径上**根本没有 LLM 请求**。
- 不会清理「嗯、那个」这类口癖，不会删掉重复或改口，不会重组结构，不会总结、翻译或润色。标点由语音识别本身给出，不额外加工。

换句话说，直接模式给你的就是识别结果本身，一个字都不多、一个字都不少。

### 交付之后的 8 秒：就地纠错

交付本身没有任何延迟，但**交付之后**多了一次机会。写入完成后的 **8 秒**内，快捷键会临时改变含义：

1. 在刚才那个应用里，**选中**识别错的那个词（双击就行）。
2. **再按一次快捷键**，说出正确的说法——比如「呸泡」被听成别的字，就说「PayPal」。
3. 松开，这段选中的文字会被替换成纠正后的结果。

浮窗上的成功提示在这 8 秒里不会消失，会显示「选中说错的词，再按一次快捷键即可纠正」并带一条走完就到期的进度条——进度条走完，快捷键就恢复原义。

几条需要知道的规则：

- **没有选中任何文字时，快捷键照常开始新一次听写。** 想接着说下一句的人不会被打断；想纠错的人先选中就行。
- **纠错成功后 8 秒重新计时**，所以连着改两三个词不用重新口述。
- **改到一半切走或改了选区就不会粘贴。** 说话到写回之间有一两秒，如果这期间你切到别的应用、或者选中了别的文字，OpenType 不会把结果贴到不该贴的地方——它会只复制到剪贴板，并告诉你原因。
- 按 `Esc`、开始新的听写、或者等它自然到期，窗口都会立刻结束。
- 这里的纠正和复核模式的语音纠错走同一条路径，因此**同样会写进实体词典**：在自己的输入框里改对一次，下次识别就直接对了。
- 只有「听写 + 直接模式」有这个窗口。问答和 Agent 的结果卡片已经有「卡片还在就继续对话」的含义，复核模式的面板本身已经占用了这个快捷键。

如果你希望在写入之前就先改一改（而不是写入之后再改），用下面的复核模式。

### 中英夹杂

MLX-Whisper 会自动检测语言，不需要提前指定。`设置 → 转写 → 转写语言` 只影响录音时实时字幕预览的语言，不影响最终识别结果。

## 5. 听写（复核模式）

复核模式是同一个「听写」模式的另一种交付方式，在 `设置 → 转写 → 听写方式` 里切换。选中后对**每一次**听写都生效，不是一次性选项。

它解决的是这个问题：语音识别把某个词听错了，而你不想为了改两个字就手动重打一遍，也不想让它被直接写进别人能看到的输入框。

### 完整流程

**1. 说话**

和平时一样按快捷键说话。松开后 OpenType **不会写入任何地方**，而是把识别结果暂存在一个浮动的复核面板里，屏幕上会显示「待复核」。

面板是非激活式浮窗：你刚才正在用的那个应用**始终保持在最前**，光标位置不会丢。

**2. 直接打字改**

面板里是一个真正可编辑的文本框，可以直接用键盘修改，和改一段普通文字没有区别。

**3. 用语音改选中的部分**

这是复核模式真正的价值所在：

1. 在面板里**选中要修改的那一段文字**。
2. 再按一次启动快捷键（此时快捷键的含义变成「为选中部分说一条修改指令」，而不是「开始一次新的听写」）。
3. 说出修改指令。
4. OpenType 会把**只针对这一段**的替换结果拼回原处，并重新选中新内容，方便你接着改。

例子：你说了「请把这笔钱通过呸泡转给他」，识别把 PayPal 听成了「呸泡」。选中「呸泡」，按快捷键，说「这个不对，应该是英文 PayPal」，面板里就会变成「请把这笔钱通过 PayPal 转给他」。

**全选整段文字**再说一条指令，就是整体重写（比如「改得正式一点」），机制完全相同，没有特殊处理。

如果按快捷键时**没有选中任何文字**，OpenType 会立刻提示「请先在复核面板中选中要修改的文字」，不会浪费一次录音。

**4. 确认或取消**

- **写入（⌘↩）**：把面板里的当前内容按正常交付路径写入（自动写入 + 剪贴板），记入历史。
- **取消（Esc）**：整段丢弃，什么都不写、什么都不复制。点击面板外侧或「取消」按钮效果相同。

**为什么是 `⌘↩` 而不是直接回车**：面板是一个多行文本框，直接回车要留给「换行」用。`⌘↩` 是 macOS 上「写完并发送」的通用做法（邮件、Slack 都是这样）。

### 需要知道的边界

- 语音纠错这一步**会调用你配置的 AI 模型**，因为它需要理解「应该是英文 PayPal」这样的指令。原始的听写本身仍然不经过模型。
- 发送给模型的只有：面板当前全文、选中区间的位置、以及你刚说的那句指令。模型只返回选中那一段的替换文本，其余部分由 OpenType 本地拼接，模型没有机会改动选区之外的内容。
- 每一轮纠错都会单独记入审计日志，见第 13 节。

## 6. 问答

### 适用情景

- 「这个单位换算怎么算来着？」
- 「Rust 里的 `Arc` 和 `Rc` 有什么区别？」
- 任何你想**直接得到答案**、而不是把自己的话整理成文字的时刻。

这是三种模式里唯一一个「回答你」而不是「保留/转换你说的话」的模式。

### 操作方式

1. 切换到 `问答` 模式。
2. 按快捷键说出问题，松开。
3. 屏幕上出现浮窗，先显示你的问题和「正在思考…」，答案返回后替换掉占位文字。
4. 答案会复制到剪贴板并停留在浮窗里；即使开启了「自动写入」，问答结果也**不会**写入当前输入框（避免把答案误发到聊天框等输入框）。

### 多轮追问：问答标签页

主窗口的「问答」标签页保存了所有历史问答会话（按最近更新排序，重启后仍在）。

**继续一段旧对话的方法**：

1. 打开主窗口 → 「问答」标签页。
2. 点开一段过去的对话，进入会话视图。
3. 保持这个会话打开，再用 `问答` 模式说话。

这一次的提问会作为**同一段对话的追问**发出去 —— 之前的每一轮真的会作为聊天历史回放给模型，而不是简单拼一段上下文摘要。所以「那它为什么更快？」这种依赖上文的问题可以正常工作。

点会话视图左上角的返回箭头，或右上角的「新对话」，就会取消关联，下一次提问重新开一段新对话。

## 7. Agent

### 适用情景

- 「帮我查一下这个库最新的稳定版本是多少。」
- 「把刚才那段结论整理成三条要点。」
- 「记住当我说 PayPal 的时候，我指的是 PayPal 这家公司。」
- 任何需要多个步骤、或者需要查点东西才能回答的任务。

### 它是非阻塞的

说完任务松开后，屏幕上会短暂显示「已下发给 Agent」，然后 OpenType 立刻回到就绪状态。你可以马上开始下一次录音，**包括下发第二个 Agent 任务**，不需要等前一个跑完。

任务完成（或失败）后：

- 弹出一条系统通知（「Agent 完成任务」/「Agent 任务失败」）。点击通知会直接打开主窗口的 Agent 标签页并展开这次任务的会话。
- 结果复制到剪贴板。
- 菜单栏 popover 在有任务运行时会显示「N 个 Agent 任务运行中」。

### Agent 标签页

主窗口的「Agent」标签页有两部分：

- **进行中**：本次启动内还没返回的任务，显示任务文字、运行状态和实时步骤日志。展开「查看步骤」可以看到 Agent 每一步做了什么（思考 / 调用工具 / 工具结果 / 完成 / 错误）。这份运行中的列表只存在内存里，退出应用后不保留。
- **历史会话**：已完成的任务会话，和问答标签页一样持久保存，重启后仍在。

### 续接一个任务

和问答完全相同的机制：打开 Agent 标签页里的某段历史会话，保持它打开，再用 `Agent` 模式说话，这次任务就会续在同一段会话里。Agent 的续接方式是把上一轮的「任务 + 结果」作为简短摘要带上（不包含上一轮的中间工具调用细节）。

### 用语音教它记住东西

**这是当前版本唯一的个性化写入方式** —— 设置里没有任何供你手动填写术语或个人资料的输入框，全部改成直接对 Agent 说。

直接在 `Agent` 模式里说「记住…」：

- **纠正一个词的写法**：「记住当我说呸泡的时候，我指的是 PayPal。」→ 写入实体词典，把「呸泡」记为 PayPal 的别名。
- **记住一条关于你的事实**：「记住我的名字叫 Diyi。」「记住我写周报的时候喜欢用短句。」→ 以自由文本形式记入本机的 owner facts。

这类明确的「记住」指令属于完全信任，会被立刻写入，不需要二次确认。

两类都能在 `设置 → 记忆面板` 里看到：第一类（术语和别名）在「实体词典」里，含别名、置信度和来源标记，可以直接在界面里新增、编辑和删除；第二类（关于你的自由文本事实）在「关于你的记忆」里，可以查看和删除（自由文本没有「改」这个操作，要换写法就删掉重说）。

你也可以说「整理一下我最近的记录」触发一次记忆整理（consolidation），效果和记忆面板里的「立即整理」按钮相同。

### 安全边界

- **Agent 的结果永远只是草稿。** 它只复制到剪贴板并显示在标签页里，**不会自动写入任何输入框**（即使「完成后自动写入」是开启的也不会）。原因很实际：一个多步任务跑完可能已经过去很久，你当初想写入的那个输入框大概率已经不是当前焦点了。
- OpenType 不会替你按回车、发送、发布或提交。任务里说「发一条推文」只表示生成草稿。
- Agent 可以调用工具。当前版本的工具通过 `OPENTYPE_MCP_SERVERS` 环境变量配置 MCP 服务，界面里没有配置入口；不配置的话 Agent 照常运行，只是没有外部工具可用。
- **工具的「只读」限制是策略，不是技术强制。** Agent Runtime 不会检查、白名单或拦截某个工具是否有副作用。产品的预期是只接入无副作用的工具（搜索、读取、查询、计算）；如果你自己接入了一个会写入外部系统的 MCP 工具，没有任何机制会阻止它执行。
- 单次任务最多循环 10 步。
- 除了 MCP 工具，还有两个始终可用的内置工具：`记住事实` 和 `立即整理记忆`，即上面那两种语音记忆写入方式。

## 8. 一句话临时切换到 Agent 模式

不改变当前选择的模式，也可以在一次口述开头直接说明本次意图：

- `Agent 模式：帮我查一下这个库最新的稳定版本。`
- `agent mode: look up the latest stable release.`

OpenType 会去掉开头的模式口令，只把后面的正文当作任务，这一次按 Agent 模式处理，界面里选择的模式不变。

兼容的口令：`agent模式`、`agent 模式`、`agent mode`、`命令输入`、`创作模式`、`command input`。口令后面必须跟一个空格或标点，且后面要有实际内容，否则不会触发。

**当前只有 Agent 模式有语音口令**，没有切换到「听写」或「问答」的口令 —— 那些旧模式的口令随模式一起删掉了。要切到这两个模式，用菜单栏 popover、主窗口的模式卡片，或按住录音快捷键点 `Tab`（见第 2 节）——后者在录音进行中也能用，切的就是这一次录音。

另外，在复核面板打开的时候，说话永远被当作**修改指令**，不会解析模式口令。

## 9. 菜单栏、主窗口和退出

OpenType 平时是一个纯菜单栏应用，没有 Dock 图标。

- **点击菜单栏图标**：永远只展开紧凑的 popover。里面只有三个模式卡片、当前状态、快捷键提示、sidecar 连接状态，以及一个「退出 OpenType」。它是一个模式切换器，不是控制中心。
- **点 popover 右上角的齿轮**：打开真正的主窗口（可缩放）。主窗口有五个标签页：输入、历史、问答、Agent、设置。
- **主窗口打开时**，应用会出现 Dock 图标，可以用 `Cmd + Tab` 切换过去。
- **关闭主窗口**，Dock 图标消失，回到纯菜单栏模式。注意这只是关窗口，**应用还在运行**，快捷键照常有效。
- **真正退出**：菜单栏 popover 底部的「退出 OpenType」，或主窗口右上角的「退出」。主窗口关闭时没有 Dock 图标也没有应用菜单，所以 popover 里那个是永远可用的退出入口。

## 10. 设置项说明

设置在主窗口的「设置」标签页里，按以下分组排列。

### 快捷键

- **启动快捷键**：见第 2 节。选中后下方会显示这个方案的说明，以及当前实际生效的状态。
- 未授权辅助功能时，这里会提示并提供「打开设置」按钮。

### 输入体验

- **完成后自动写入当前输入框**：开启后处理完成会自动粘贴到当前获得焦点的输入框；关闭则只复制到剪贴板。无论开关状态，结果都会保留在剪贴板。Agent 模式不受这个开关影响（永远只复制）。
- **保留本地输入历史**：开启后在本机保存最近 **100 条**输入记录（模式、结果、所用应用、时间），可在「历史」标签页复制或重新使用。
- **静音**：关闭全部操作提示音。
- **OpenType Air · 试听**：四个同一音色家族的低响度提示音，分别对应「开始说话 / 结束录音 / 完成写入 / 出现问题」。可以逐个试听。麦克风就绪音播放后即可开始说话；松开快捷键时立即播放结束音，不需要等处理完成。模式切换只有视觉反馈，不发声。
- **录音时显示实时字幕**：只控制录音时的临时预览，不改变最终识别和处理。开启但未授权语音识别时，这里会提供授权按钮。

### 记忆与偏好

- **启用本地长期记忆与个性化**：开启后，每次完成的正式文字任务会在本机数据库追加一条可追溯记录（原始转写、实际指令、当时的上下文、结果、模式、应用、时间）。**不保存原始录音**。

  **这个开关只管 Swift 侧这一份记录（`memory.sqlite3`）。** sidecar 侧另有一份独立的事件记录（`opentype.sqlite3`），`听写`、`问答`、`Agent` 三种模式完成时都会各写一条，**不受这个开关影响**。注意「记下来」和「发出去」是两件事：听写的那一条**只存在本机**，永远不会成为「记忆整理」的原料，也就不会被发给模型（见第 13 节）。
- **每 100 条更新「已学到的偏好」**：开启后，每积累 100 条记录，OpenType 会在**完全离线**的情况下归纳一次常用术语、常见任务、语言习惯和表达偏好。这是低权重推断，仅作参考。下方会显示距离下一次更新还差多少条。
- **已学到的偏好**：上面那次归纳的结果，只读展示。
- **最近 Agent 任务**：最近三条 Agent 任务的请求和结果预览。
- **显示数据库**：在 Finder 中定位本机数据库文件。

说明：这套「已学到的偏好」只在本机记录与展示，**不会被注入到发给模型的上下文里**。这不是「还没做」——2026-08-14 的清理里，本来打算做这件事的那层代码被整体删掉了，因为真正参与 prompt 的是 sidecar 侧的那套记忆（下面的「记忆面板」），一份记忆到不了模型、另一份到得了，只会让人误判。`问答`/`Agent` 实际发给模型的内容以第 13 节为准——本次输入、sidecar 侧记住的 owner facts 与相关术语，以及续接会话时的历史轮次，与这里的 Swift 侧「已学到的偏好」是两回事。

### 记忆面板

sidecar 侧记忆服务的管理窗口，与上面那套 Swift 侧的任务记录是**两套独立的存储**：

- **实体词典**：术语的标准写法、别名和置信度。来自你对 Agent 说的「记住…」、复核模式里的语音纠错，以及自动整理过程的产出。**可以直接在这里增删改**：顶上一行填词条和别名（逗号分隔，中英文逗号都认）点「添加」；每行的铅笔按钮就地改写法、别名和置信度，垃圾桶按钮删除。改完立刻生效，会影响后续识别与纠错。
- **关于你的记忆**：你让 Agent 记住的自由文本事实。可以查看和删除。
- **来源标记**：上面两类的每一条都带一个来源徽章 —— 「你确认过」（你亲手写的或亲手纠正的）、「自动整理」（整理过程归纳的）、「助理写入」、以及橙色的「未经确认」。**「未经确认」表示这条是 Agent 在任务过程中写下的，内容可能来自它读到的网页或文件，而不是你说的话** —— 这个标记就是让你来审的，看着不对就删掉。反过来，你亲手为一条「未经确认」的词条背书之后 —— 用「添加」重新写一遍同名的词条，或者在复核模式里把它当成纠正目标说一次 —— 它会升级成「你确认过」，并且不会再被降回去。（就地用铅笔改写法不算背书，不改来源标记。）
- **整理记录**：过去每一次记忆整理（consolidation / 「dreaming」）的时间和摘要。
- **立即整理**：手动触发一次整理（这个按钮，或对 Agent 说「整理一下我最近的记录」），无条件立刻跑一次。

**整理现在也会自己发生（2026-08-14 起）。** 之前只有你按按钮才整理，现在 sidecar 启动 **5 分钟**后会自查一次：如果**距上次整理已超过 12 小时**、并且**积累了至少 5 条可整理的记录**，就自动跑一次。每次启动只查一次，不是定时轮询。同一批改动还让 `问答` 的记录也进入原料池（此前只有 `Agent` 任务）。

**整理是一次真实的模型调用，会花你的 API 额度。** 它会把最多 200 条未整理记录、以及现有词典的词条和别名，打包发给你在「AI 模型」里配置的那个服务。

**但听写的记录不在其中。** `听写` 的内容会记在本机，却**永远不会进入整理的原料**，因此不会因为整理而被发给模型 —— 这条产品承诺不因为「只是整理时才发」而打折扣：延迟发送也是发送。所以自动整理实际送出去的，只有 `问答` 和 `Agent` 的记录，而这两种模式的文字本来就是你主动交给模型的。

想让整理也从你的听写里学新词（比如你从没纠正过、但反复出现的人名），这需要一个明确的开关来交换这份承诺，当前版本**没有**提供，也不会偷偷替你打开。不过词典本来就会从听写里学 —— 见 §4「交付之后的 8 秒」：你每纠正一次，那个词就**在本机**进了词典，全程不经过模型。

删除会先弹一次确认，因为**删除不可撤销**。整理记录本身仍然没有回滚按钮 —— 一次糟糕的整理目前还是无法从界面整体撤销，只能逐条改或删。

### 转写

- **听写方式**：`直接模式` / `复核模式`，见第 4、5 节。
- **转写语言**：`自动识别（支持混合语言）`（默认）或指定单一语言（中文、粤语、英语、日语、韩语、德语、法语等近 30 种）。

**这个选项只影响录音时 Apple 实时字幕预览的语言。** 最终识别由 MLX-Whisper 自动检测语言，不接收这个参数。中英夹杂请保持「自动识别」。

### 语音识别

选择最终识别在哪里跑：

- **本机（MLX-Whisper）**（默认）：完全在本机运行，不联网，不需要 API Key。仅支持 Apple Silicon。
- **远程 API**：填 Base URL、API Key 和模型名（默认 `whisper-1`）。实现基于 OpenAI 的 `/v1/audio/transcriptions` 接口形状，因此**不限于 api.openai.com** —— 任何兼容这个接口的自建服务都可以填进来。填完可以「测试连接」，再「保存」。

切换到远程后，音频才会离开本机。

### AI 模型

`问答`、`Agent`，以及复核模式下的语音纠错都会用到这里配置的模型。

- **提供方**：`Anthropic`（Messages API）或 `OpenAI 兼容`（Chat Completions，覆盖 DeepSeek、OpenAI 本身以及自建兼容服务）。
- **Base URL** 和 **API Key**。
- **测试连接**：成功后会自动拉取该服务的模型列表，从下拉框里选模型，而不是盲填模型名。如果这个服务不提供模型列表接口，会退回到一份常见模型的内置清单，界面上会明确标注「可能不完整或已过期」。
- **保存**。

已保存的 API Key **不会回显**，界面里只显示掩码形式（如 `sk-b...81ff`）。因此重新保存这一节配置时需要重新输入一次 Key。

### 连接与权限

麦克风、辅助功能、实时字幕三项权限的当前状态，未授权时可直接跳转授权。

### 隐私与数据

**本地数据管理**（默认收起，高风险操作）：

- **重置输入历史**：清空本机保存的输入记录，需二次确认，不可撤销。
- **重新学习偏好**：移除本地任务记录和系统推断，需二次确认，不可撤销。

这两项都不会影响审计日志（见第 13 节），也不会影响 sidecar 侧的任何数据——**实体词典、关于你的记忆，以及那份三种模式都会写入的事件记录都留在原处**。词典和「关于你的记忆」可以在上面的记忆面板里逐条删除；事件记录目前**没有任何界面可以查看或清空**，只能直接删掉 `opentype.sqlite3`（连同词典和会话记录一起丢失）。

## 11. 状态提示是什么意思

| 提示 | 含义 |
| --- | --- |
| 就绪 | 空闲，等待快捷键 |
| 已切换模式 | 模式切换成功，下一次输入使用显示的模式。**录音进行中的切换不会显示这一条**——那时改的是这一次录音本身，胶囊会直接换成新模式（见 §2） |
| 正在听 | 正在录音 |
| 正在识别 | 正在把音频转成文字 |
| 正在整理 | 正在调用 AI 模型处理 |
| 正在写入 | 正在把结果放入当前输入框 |
| 完成 | 结果已写入。直接模式下这条提示会多停留 8 秒，带一条进度条和「选中说错的词，再按一次快捷键即可纠正」——进度条走完之前都可以就地纠错（见 §4） |
| 已复制 | 自动写入没有成功（或未开启），但结果已安全复制到剪贴板。直接模式下同样带上面那 8 秒的纠错提示 |
| 已下发 | Agent 任务已交给后台，或听写结果已进入复核面板 |
| 未执行 | 本次没有产生任何写入，例如复核面板取消，或纠错时没选中文字 |
| 出现问题 | 下方会显示具体原因和下一步操作 |

## 12. 常见使用组合

### 微信和即时聊天

`听写` + 直接模式，长按左 `Option` 说一两句话，松开直接写入。

### 口述文章或长笔记

`听写` + 直接模式，双击左 `Option` 后持续口述，完成时按任意普通键。

### 口述包含大量专有名词的内容

先用 `听写` + **直接模式**——照常说、照常写入，然后利用交付后 8 秒的窗口：选中被听错的人名或产品名，再按一次快捷键说出正确说法，就地改掉（见 §4「交付之后的 8 秒」）。大多数情况下只有一两个词需要改，这样比每次都过一遍面板快得多，而且改完自动进词典，下次识别就对了。

只有当**几乎每句都要改**、或者这段文字在写进别人能看到的输入框之前必须先审一遍时，才值得切到 **复核模式**：说完先在面板里检查，把错的地方选中并用语音纠正，确认无误再 `⌘↩` 写入。

### 随手查一件事

`问答`，说出问题，答案直接出现在浮窗里，同时进剪贴板。

### 连续追问同一件事

打开主窗口 →「问答」标签页 → 点开刚才那段对话 → 保持打开，用 `问答` 模式继续说。

### 下发一个需要花点时间的任务

`Agent`，说完就继续干别的。完成时会有通知，点通知直接跳到结果和步骤日志。

### 教 OpenType 记住你的用词

`Agent`，说「记住当我说 X 的时候我指的是 Y」。在 `设置 → 记忆面板` 里能看到写入结果。

## 13. 隐私与数据

- **音频默认不出本机。** 语音识别默认由本机 MLX-Whisper 处理；只有当你在「语音识别」里主动切换到「远程 API」，音频才会发送到你自己填写的地址。处理结束后本地临时音频文件会被删除，**原始录音从不写入任何数据库或记忆**。
- **`听写` + 直接模式完全不经过任何 AI 模型。** 识别到什么就是什么，没有任何文字被发给模型。**唯一的例外是你主动发起的就地纠错**（§4「交付之后的 8 秒」）：只有当你在那 8 秒内选中文字并再次按下快捷键时，被选中的那段文字和你说的修改指令才会发给模型——不选、不按，就什么都不发。
- **听写会被记在本机，但不会被发出去（2026-08-14 起）。** 每次成功的听写会在 sidecar 侧存一条记录（原始识别文本 + 词典替换后的文本）。这条记录**只留在你的机器上**：它被明确排除在「记忆整理」的原料之外，所以不会因为整理而被发给模型。上面那句「没有任何文字被发给模型」在加入这份记录之后依然成立 —— 这是刻意保证的，不是巧合。
- **`问答` 每次问答也会在 sidecar 侧记一条事件**（问题 + 答案）。这一条**会**进入整理原料池 —— 但这两段文字本来就是你主动发给模型的，整理没有扩大它的去向。
- **`问答`、`Agent`，以及两种语音纠错（复核面板里的和直接模式的就地纠错）**会把相关文字发给你自己配置的 AI 模型。发送内容包括：本次输入；**你亲口让 Agent「记住」的全部 owner facts（"关于我" 类事实）——是全部注入，不是按相关性筛选**（只排除非 owner 来源的不可信事实，见下）；出现在本次输入里的已知术语；以及（续接会话时）这段会话的历史轮次。（两种语音纠错都是例外：它们只发送待改的文字 + 选区 + 你说的指令，不带任何记忆上下文。）
- **实时字幕预览**用的是 Apple 系统自带的本机语音识别，只作为录音时的临时预览。
- **API Key 保存在本机 sidecar 的数据目录下，是 `chmod 600` 的明文 JSON 文件**（仅当前系统账户可读写），不写入代码仓库、日志或历史，接口回显时只显示掩码。这不是 Keychain 级别的硬件隔离，信任边界等同于你的本机账户本身 —— 如果这台机器的账户本身不可信，这份配置就不安全。
- **本机存储位置**：全部在 `~/Library/Application Support/OpenType/` 下。
  - `history.json` —— 输入历史
  - `memory.sqlite3` —— Swift 侧的任务记录与已学到的偏好
  - `opentype.sqlite3` —— sidecar 侧的实体词典、owner facts、问答/Agent 会话记录，以及 **`听写`/`问答`/`Agent` 三种模式的事件记录**（2026-08-14 起三种模式都写；此前只有 `Agent`）。其中只有 `问答`/`Agent` 的记录会作为「记忆整理」的原料发给模型，`听写` 的那份纯本地留存。这份事件记录**目前没有界面可以查看或清空**，第 10 节的两个重置动作也不会清它 —— 想彻底清掉只能删除这个文件本身（会连词典和会话记录一起丢失）。
  - `provider-config.json` —— 语音识别与 AI 模型配置（含 API Key）
  - `audit-events.v1.jsonl` —— 审计日志
  - `context-debug.log` —— sidecar 的上下文使用调试日志：每次 `问答`/`Agent` 请求会追加一行，含**截断到约 200 字的输入文本**和本次匹配到的术语/owner facts 数量。它不轮转，**也不受第 10 节「重置输入历史」影响**（重置只清 `history.json` 和 Swift 侧任务记录）。
- **审计日志是只追加的，永不改写。** 每一次识别、每一次完成、取消或失败都会追加一条。它不受「重置输入历史」和「重新学习偏好」影响。
- **复核模式的纠错链会被完整记录。** 这是值得单独说明的一点：一次复核会话不只记录最终结果，而是记录**每一轮纠错** —— 原始识别结果、你说的每一条修改指令、被替换的那段原文、模型给出的替换文本，以及最终是写入还是取消。这些事件通过 `supersedesEventId` 串成一条链，可以完整回放整个会话的每一步，而不是只看到最后一个状态。
- **OpenType 从不替你按回车、发送或发布。** 所有模式的结果都只是草稿。
- **两种「归纳」要分开看，只有一种在本机。**
  - **Swift 侧的「已学到的偏好」**（每 100 条记录重算一次，第 10 节）：**完全在本机进行**，一次模型调用都不发。
  - **sidecar 侧的「记忆整理」/ dreaming**：**会调用模型**。它把最多 200 条未整理记录、加上现有词典的词条与别名，一起发给你配置的 AI 模型服务。2026-08-14 起它除了手动按钮之外还会自动触发（sidecar 启动 5 分钟后自查，满足「≥12 小时 + ≥5 条可整理记录」就跑一次），所以这是唯一一处**不需要你做任何动作**就会发生的模型调用。**但它送出去的只有 `问答` 和 `Agent` 的记录 —— `听写` 的记录被排除在外，永远不参与。** 整理结果（词条与别名）写在本机，可在记忆面板里逐条审阅和删除。

## 14. 使用原则

如果只记住四件事：

1. 短内容长按快捷键，长内容双击快捷键；按住录音快捷键点 `Tab` 换模式（录音中也能切，切的就是这一次）。
2. `听写` 一个字都不会改；想改就用复核模式，选中一段、按快捷键、说出修改要求。
3. `问答` 和 `Agent` 都能续接旧会话：先在对应标签页里打开那段对话，再说话。
4. 所有结果都只是草稿，都在剪贴板里，发送与否永远由你决定。
